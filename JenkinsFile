pipeline {
    agent any
    tools {
        nodejs 'node-20'
        xvfb 'xvfb'
    }
    stages {
        stage('Clone Project') {
            steps {
                git branch: 'qa', url: 'https://github.com/PaulLandaeta/isc-system-web.git'
            }
        }
        stage('Install Dependencies') {
            steps {
                sh 'npm install'
            }
        }
        stage('Build') {
            steps {
                sh 'npm run build'
            }
        }
        stage('Run Cypress') {
            steps {
                sh ls -a
                sh 'npx cypress run --spec "cypress/e2e/**/*.cy.{ts,tsx}"'
            }
        }
    }
}
