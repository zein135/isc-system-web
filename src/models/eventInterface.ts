export interface EventDetails {
    title: string;
    date: string;
    duration: string;
    scholarshipHours: string;
    location: string;
    maxParticipants: number;
    maxSubstitutes: number;
    description: string;
}